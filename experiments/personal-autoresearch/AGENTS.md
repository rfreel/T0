# Agent instructions

Read `PROGRAM.md` before changing this project. Treat `prepare.py`, `evaluate.py`, `run.py`, `config.json`, `target/OBJECTIVE.md`, `target/CONSTRAINTS.md`, and `target/catalog.json` as human-owned locked files.

For an experiment, edit only the files declared mutable in `config.json`. Run the unit tests and the smoke test after changing harness code. Never put personal information, credentials, tokens, or private inventories into committed files.
