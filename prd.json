{
  "version": 1,
  "items": [
    {
      "id": "SHELL-001",
      "title": "Scaffold canonical loop center/radius/tangent",
      "acceptance": [
        "loop/ exists with GOAL.md, RULES.md, log.txt, last_error.txt",
        "scripts/ralph/loop.sh and radius.sh exist",
        "CI runs and blocks invalid loop state",
        "progress.txt exists and receives a first entry"
      ],
      "passes": true,
      "notes": "Implemented loop center files, radius enforcement scripts, CI back-pressure, and pre-commit config."
    },
    {
      "id": "SHELL-010",
      "title": "Top10 scripts + CI wiring",
      "acceptance": [
        "loop.sh orchestrates PATCH-ONLY pipeline with llm_stub.sh",
        "invariants.sh enforces required files, JSON validity, conflict markers, and radius",
        "CI runs script contract checks, invariants, ci_gate, and pre-commit"
      ],
      "passes": true,
      "notes": "Added top10 scripts, llm_stub contracts, and wired invariants/ci/pre-commit gates."
    }
  ]
}
