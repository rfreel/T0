# UTP Usage

```bash
pnpm utp:run --source examples/utp/prompt-stack/inputs/source.txt --source-domain prompt-stack --target-domain prompt-stack --target-env local
pnpm utp:audit --run runs/<utc-iso>
pnpm utp:replay --run runs/<utc-iso>
```
